1. values added: 20
2. final result: 20
3. We should not use var because it allows us to access a variable anywhere 
   within the function it is in, so it's easy to make mistakes if we use the 
   same name for different variables within the same function but in different
   blocks.
4. values added: 20
5. Returns an error, because console.log() won't see result, as it is declared
   with a let keyword, so it is only visible within the if block.
6. values added: 0
7. Returns an error, because console.log() won't see result, as it is declared
   with a const keyword, so it is only visible within the if block.
